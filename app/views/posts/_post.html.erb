<% @posts.each do |post| %>
  <div class="card">
    <h5>Posted on <%= post.created_at.in_time_zone("Central Time (US & Canada)").strftime("%b %d,  %Y at %l:%M %p") %></h5>
    <p><%= post.content %></p>
  </div>
<% end %>